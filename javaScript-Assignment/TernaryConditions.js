// Question - 1

/* 1. Write a program that takes in a number and outputs whether it is positive, negative, or zero. */

// code

let num = 1;

num == 0 ? console.log('The number is zero') : num > 0 ? console.log("The number is greater than zero") : console.log(`The number is lesser than zero`);

// Question - 2

/* 2. Create a program that takes in two numbers and prints the larger one. */

// code

let num1 = 10;
let num2 = 12;

num1 == num2 ? console.log(`Both the numbers are equal`): num1 > num2 ? console.log(`The larger number among the two number is ${num1}.`): console.log(`The larger number among the two number is ${num2}.`);